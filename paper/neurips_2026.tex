\documentclass{article}

\PassOptionsToPackage{round,authoryear}{natbib}
\makeatletter
\def\input@path{{starter/}}
\makeatother
\usepackage[dblblindworkshop]{neurips_2026}

\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{url}
\usepackage[hidelinks]{hyperref}

\graphicspath{{figures/}}
\newcommand{\model}{Qwen2.5-Math-1.5B-Instruct}
\newcommand{\dataset}{ProcessBench}

\setlength{\parskip}{0pt}
\setlength{\bibsep}{2.5pt}

\title{Decodability Is Not Localization:\
Auditing Hidden-State Error Probes in Qwen2.5-Math-1.5B-Instruct}
\workshoptitle{Interpretability for Discovery}
\author{Anonymous Author(s)}

\begin{document}
\maketitle

\begin{abstract}
We audit whether hidden-state probes detect the first invalid step in a mathematical reasoning
trace, or instead exploit easier correlates of an already-invalid prefix. In a case study of
\model{} on \dataset{}, a linear probe predicts the persistent target \emph{invalid so far}
with held-out AUROC $0.866$. Equally tuned nuisance baselines explain part of that margin:
prefix TF--IDF reaches $0.751$, structural metadata $0.776$, and their joint model $0.810$.
Adding final-answer correctness---a benchmark annotation, not an online signal---yields AUROC
$0.874$, numerically above the hidden state. A nested comparison then tests whether hidden
features add incremental value \emph{conditional on} nuisances. Adding hidden states to the
best nuisance model improves held-out AUROC by $+0.0587$ ($95\%$ CI $[0.0414,0.0745]$), log
loss by $-0.0798$ ($[-0.1038,-0.0548]$), and Process F1 by $+0.1866$
($[0.1397,0.2340]$); the oracle-assisted joint model shows a smaller gain of $+0.0348$
AUROC. Hidden states thus carry incremental predictive information beyond text and structural
nuisances on this split. The gain does not translate to reliable localization: exact first-error
detection is $28.9\%$, correct-trace rejection is $61.2\%$, and detected errors arrive late by
$0.59$ steps on average. A post-hoc transition probe reaches AUROC $0.769$ on matched
held-out pairs, but placebo reuse and covariate imbalance keep it exploratory. Two behavioral
readouts fail their validity gates, so the causal section is empty by design. We contribute an
evaluation audit with a mixed result: hidden states add incremental information over tuned
nuisances, but the evidence is conditional on one model, one split, and one benchmark, and it
does not establish localization, calibration, or causal use.
\end{abstract}

\section{Introduction}

Process supervision asks whether each step of a solution is valid, rather than whether the final
answer happens to be correct \citep{cobbe2021verifiers,lightman2023verify}. A related question is
whether a model's hidden representation changes when it reads a reasoning step that has become
invalid. Probe accuracy alone cannot answer it: if every boundary after the first error is labeled
positive, a classifier may recognize accumulated inconsistency, solution length, generator style,
or final-answer outcome without responding at the onset.

The \emph{Interpretability for Discovery} framing makes the stakes explicit: a linearly decodable
pattern is a \emph{candidate}, not yet discovered knowledge. Turning a candidate into a claim
requires validation at distinct levels, and failing any one changes what the pattern can be used
for. This paper is a worked example of that validation sequence. We test four claims that are
often collapsed: (i) invalid prefixes are linearly decodable; (ii) a score localizes the first
error; (iii) the signal transfers across benchmark sources; and (iv) the model behaviorally uses
the decoded direction. We test them in one bounded setting: \model{} reading \dataset{} traces
\citep{yang2024qwenmath,zheng2025processbench}, with problem-grouped splits and a persistent
invalid-so-far target, audited by prefix-text, metadata, and joint nuisance controls fit under
the same validation budget as the probe, source and generator stratification, an onset-aligned
analysis, and a matched transition probe.

The audit produces a mixed result. The hidden-state probe's pooled AUROC is high, but an equally
tuned joint shortcut model combining prefix text, structural metadata, and final-answer correctness
also reaches a high AUROC. A nested comparison then shows that adding hidden features to the best
nuisance model improves held-out ranking and proper scores on the inspected split. What survives
is a narrower, task-specific advantage: incremental predictive information over nuisances, with
some localization gain offset by worse correct rejection. The behavioral assay is uninterpretable.
We contribute a documented evaluation audit: what a hidden-state pattern can and cannot support
after several rounds of controls, and a concrete checklist for when an internal pattern is safe
to treat as a candidate discovery.

\section{Related work and contribution boundary}

External process reward models learn step quality from human or model supervision
\citep{lightman2023verify,li2023stepaware}. \dataset{} annotates the earliest error in 3,400
model-generated traces from GSM8K, MATH, OlympiadBench, and Omni-MATH
\citep{zheng2025processbench,hendrycks2021math,he2024olympiadbench,gao2024omnimath}. We repartition
these benchmark sources for representation analysis; they are heterogeneous sources, not
independent domains or evidence of model-family generalization.

Closest to this work, Sun et al.\ probe arithmetic errors and use the probe for selective
re-prompting; ReProbe learns hidden-state step scorers for test-time scaling; and STEP uses
hidden-state scores to prune reasoning traces
\citep{sun2025arithmetic,ni2026reprobe,liang2026earlysignals}. Table~\ref{tab:delta} states the
comparison at the level needed for our claim: our novelty is not that hidden states predict
reasoning quality, but the joint audit of a human first-error target using first-crossing,
matched-transition, source-transfer, equally tuned nuisance, and failed-assay evidence.

\begin{table}[t]
\centering
\small
\caption{Closest hidden-state verifier work. ``Use'' records the reported downstream or causal
test, not a judgment of overall paper quality.}
\label{tab:delta}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lllll}
\toprule
Work & Supervision / task & Scope & Onset evaluation & Use test \\
\midrule
Sun et al. & arithmetic error & addition, GSM8K & no first-error metric & re-prompting \\
ReProbe & step credibility & math, planning, QA & step scoring & test-time scaling \\
STEP & step quality & reasoning traces & early step scoring & pruning \\
Ours & human first error & four ProcessBench sources & first crossing + transition & failed pilot \\
\bottomrule
\end{tabular}
\end{table}

Probing work cautions that decodability need not imply behavioral use
\citep{hewitt2019control,belinkov2022probing,elazar2021amnesic}; an additive intervention is
evidence only if its behavioral readout first distinguishes the relevant baseline states.

\section{Experimental design}
\label{sec:method}

\subsection{Data flow, model, and split}

Each \dataset{} trace supplies a problem, generator identity, written steps, source, final-answer
correctness, and a human first-error index $e_i$, with $e_i=-1$ for fully correct traces. Traces
sharing normalized problem text form a group. Seed 42 assigns groups within source $\times$
error-status strata to 60\% train, 20\% validation, and 20\% test: of 3,400 attempted traces,
3,360 are retained (2,187 with an annotated error), yielding 24,909 written-step boundaries and
669 held-out traces. A 2,048-token limit excludes a complete trace rather than truncating it.
Appendix~\ref{app:data} gives the full attempted/retained accounting by split and source.

We run \model{} in bfloat16 and record its 1,536-dimensional residual-stream state at each written
step and each of 29 hidden-state indices. The completed extraction used batch size 16 on one
NVIDIA A100 GPU. The model reads each trace's problem and numbered steps in a single causal
forward pass; it does not generate the traces. Locked package versions, compute, storage, and
licenses appear in Appendix~\ref{app:repro}; artifact identity fixes the model, context length,
dtype, and dataset SHA-256.

\subsection{Persistent target, probe, and trace-level estimands}

At boundary $k$ of trace $i$, the frozen primary target is
\begin{equation}
y_{ik}=\mathbb{1}[e_i\geq 0\ \wedge\ k\geq e_i].
\label{eq:target}
\end{equation}
It means \emph{invalid so far}; it does not label only the first invalid step. A class-balanced,
L2 logistic probe is fit separately at each index after train-set standardization. Validation AUROC
selects $C\in\{0.01,0.1,1,10\}$; validation Process F1 selects a threshold on a 0.005 grid from
0.05 to 0.95 and then the layer. The selected index is 23, $C=0.01$, threshold $0.645$; it is refit
on train plus validation and evaluated once on test.

The predicted first error is the first threshold crossing, or $-1$ if none occurs. We report
boundary AUROC and average precision, exact error-boundary accuracy among erroneous traces,
correct-trace rejection, their harmonic mean (Process F1), complete-trace accuracy, and detection
within one or two steps; the localization metrics weight traces equally. Primary intervals use
1,000 whole-trace bootstrap draws; the post-hoc paired comparisons use 2,000 draws because they
were added in a later audit configuration, which we state rather than presenting one frozen
protocol.

\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{method_and_trajectory.pdf}
\caption{The persistent target and first-crossing evaluation. A causal forward pass produces one
boundary state per step. In the example, the score crosses one step after the annotated error.}
\label{fig:method}
\end{figure}

\subsection{Shortcut, joint-control, and nested-comparison audits}
\label{sec:controls}

Every nuisance control receives the same model-selection budget as the hidden-state probe: L2
regularization is selected from $\{0.01,0.1,1,10\}$ by validation AUROC, the threshold is selected
on the same validation scores, and the selected model is refit on train plus validation before the
single test evaluation. Each control is also fit with sample weights inverse to boundary counts,
so every trace contributes equal total training loss, mirroring a trace-equal objective rather
than reweighting evaluation after fitting. Current-step TF--IDF uses the destination step; prefix
TF--IDF uses the problem and all reasoning text through the current boundary; structural metadata
uses source, generator, absolute and fractional step position, trace length, and token count. Two
\emph{joint} baselines combine prefix text and all structural metadata.

Final-answer correctness is a benchmark annotation, not an online signal: it records whether the
trace's final answer matches the benchmark reference solution, for every trace regardless of where
an error occurs. We use it as a diagnostic upper bound on nuisance predictability, not a deployable
baseline. The oracle variant adds this field to the joint model to test whether hidden states
carry information \emph{beyond} what the outcome field already reveals.

A shuffled-label hidden probe checks the fitting procedure. We report paired
probe-minus-control intervals from whole-trace resampling, pooled and within
final-answer-correctness strata. The primary objective weights each boundary equally in
\emph{evaluation}; the trace-equal refit above addresses the training asymmetry. Descriptive
subgroup outcomes stratify by source, generator, final-answer correctness, error position, and
length and token-count quartiles.

\subsection{Nested nuisance-plus-hidden comparison}
\label{sec:nested}

Incremental information requires comparing a nuisance-only model with the same model augmented by
hidden features. We define three feature sets:
\begin{itemize}
\item[$N$:] prefix TF--IDF plus structural metadata, fitted with the protocol above;
\item[$N{+}H$:] the same nuisance features plus the hidden-state probe score at the selected
  boundary;
\item[$O{+}H$:] the oracle nuisance features (including final-answer correctness) plus the
  hidden-state score.
\end{itemize}
Each model is fit with the same L2 grid, validation selection, trace-equal training weights, and
refit-on-train-plus-validation protocol. The comparison $N{+}H$ minus $N$ measures whether hidden
features add value \emph{conditional on} the best deployable nuisance model. The comparison
$O{+}H$ minus $O$ measures the same for the oracle-aided model. Both use 2,000 whole-trace
bootstrap paired draws.

\subsection{Post-hoc onset analyses}

The following analyses were designed after the primary test result and are labeled post-hoc. We
circularly shift each frozen score trajectory 5,000 times, preserving its values while breaking
alignment to the annotation. We compare the score jump at each noninitial error with a transition
from a fully correct trace, preferring the same source and generator and then minimizing
differences in relative position, trace length, and token count.

The transition probe is a separate analysis of $h_{i,e_i}-h_{i,e_i-1}$. Every eligible erroneous
transition is paired with a correct-trace transition in the same frozen partition, giving 1,152
train, 387 validation, and 380 test pairs; the pairing reuses correct traces (median 2 pairs per
placebo trace, 90th percentile 6, maximum 46, from 687 unique placebos), and matched groups are
close on relative position but retain token-count and length differences (standardized
differences $0.28$ and $0.15$). Layer and $C$ are selected by validation AUROC; index 21 and
$C=0.01$ are selected. Controls predict from position, destination-step TF--IDF, or shuffled
labels. The protocol is fully specified and executable, but the test set was not untouched when
the analysis was conceived, so the result is exploratory regardless of its interval.

\section{Results}

\subsection{Hidden states add incremental information over nuisances}

The selected probe reaches AUROC $0.866$ (95\% CI $[0.849,0.884]$), average precision $0.843$
($[0.813,0.870]$), and step F1 $0.743$ ($[0.716,0.769]$). Within erroneous traces, first-step
centering leaves pooled AUROC at $0.881$ ($[0.862,0.900]$), and mean per-trace AUROC is $0.968$
($[0.958,0.976]$). Stable trace offsets therefore do not explain all discrimination.

Table~\ref{tab:controls} shows the equally tuned comparison. Prefix text reaches AUROC $0.751$,
structural metadata $0.776$, and their joint model $0.810$: the controls' errors are partly
complementary. The two oracle-augmented models reach $0.854$ (metadata plus outcome) and $0.874$
(joint plus outcome), with the latter numerically above the hidden probe. Under equal tuning
budgets, the separate hidden-only and nuisance-only comparisons do not establish incremental
information; that requires the nested comparison.

\begin{table}[t]
\centering
\small
\caption{Held-out comparison generated from the audited metric schema. Controls use the same
validation $C$ grid, validation threshold selection, and trace-equal training weights; the frozen
hidden probe retains its original boundary-equal fit. Paired intervals are hidden minus control;
positive $\Delta$ favors the hidden state. Oracle models include final-answer correctness and are
labeled accordingly.}
\label{tab:controls}
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrrl}
\toprule
Predictor & AUROC & Proc.\ F1 & Error exact & Corr.\ rej. & $\Delta$ AUROC [95\% CI] \\
\midrule
Hidden state & .866 & .393 & .289 & .612 & --- \\
Prefix TF--IDF & .751 & .274 & .192 & .477 & .115 [.089,.142] \\
Structural metadata & .776 & .168 & .148 & .194 & .090 [.069,.110] \\
Joint text + metadata & .810 & .210 & .169 & .278 & .056 [.037,.073] \\
\midrule
Metadata + final outcome\textsuperscript{$\dagger$} & .854 & .262 & .164 & .646 & .012 [$-$ .012,.036] \\
Joint + final outcome\textsuperscript{$\dagger$} & .874 & .294 & .176 & .895 & $-$ .008 [$-$ .028,.013] \\
\bottomrule
\end{tabular}

\footnotesize $\dagger$ Oracle models include the benchmark-annotation final-answer
correctness field, which is not available at inference time.
\end{table}

The nested comparison in Table~\ref{tab:nested} answers the incremental-information question
directly. Adding the hidden-state score to the best deployable nuisance model ($N{+}H$ minus $N$)
improves AUROC by $+0.0587$ ($95\%$ CI $[0.0414,0.0745]$), log loss by $-0.0798$
($[-0.1038,-0.0548]$), and Process F1 by $+0.1866$ ($[0.1397,0.2340]$). Adding hidden features
to the oracle model ($O{+}H$ minus $O$) gives a smaller AUROC gain of $+0.0348$
($[0.0242,0.0451]$), consistent with the outcome field already carrying most pooled information
when it is informative. Within final-answer-correctness strata, the hidden AUROC advantage is
$0.073$ ($[0.039,0.107]$) among correct-outcome traces and vanishes among incorrect-outcome
traces ($-0.001$ [$-0.022,0.021]$).

\begin{table}[t]
\centering
\small
\caption{Nested nuisance-plus-hidden comparison. All models use the same selection budget,
trace-equal training weights, and refit-on-train-plus-validation protocol. $\Delta$ is the
hidden-augmented model minus its nuisance-only counterpart.}
\label{tab:nested}
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{llrrrl}
\toprule
Nuisance set & Model & AUROC & Log loss & Proc.\ F1 & $\Delta$ AUROC [95\% CI] \\
\midrule
$N$ (text + metadata) & $N$ only & .810 & .444 & .210 & --- \\
 & $N{+}H$ & .866 & .364 & .393 & $+$.057 [.037,.073] \\
\midrule
$O$ (text + metadata + outcome) & $O$ only & .874 & .384 & .294 & --- \\
 & $O{+}H$ & .882 & .364 & .401 & $+$.035 [.024,.045] \\
\bottomrule
\end{tabular}
\raggedright\footnotesize The $O$ model includes final-answer correctness (not available at
inference time) as a diagnostic upper bound. All $\Delta$ intervals exclude zero.
\end{table}

The decomposition matters more than the pooled number. The hidden probe retains a Process F1
advantage over the oracle joint model of $0.099$ ($[0.040,0.158]$) and an erroneous-trace
exact-localization advantage of $0.113$ ($[0.064,0.163]$). This gain is offset by lower correct
rejection: $0.612$ for the hidden probe versus $0.895$ for the oracle-assisted joint model
(hidden-minus-joint difference $-0.283$ $[-0.347,-0.218]$). On the 237 correct traces, both
reject 140, only the hidden probe alarms on 72, only the joint model alarms on 5, and both
alarm on 20. The models trade erroneous-trace onset localization against correct-trace rejection.

Equal trace weighting at evaluation changes AUROC by $+0.001$ (95\% paired CI $[-0.007,0.010]$).
Complete-trace accuracy falls from $0.491$ in the shortest length quartile to $0.264$ in the
longest, with heterogeneous source-level estimates (Appendix~\ref{app:subgroups}).

\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{predictive_results.pdf}
\caption{Layer-wise held-out performance and original controls. The embedding-index row is a
linear probe at hidden-state index 0, included as a depth comparison rather than a lexical control.
Process F1 is N/A within error-only traces because correct-trace rejection is undefined. The
equally tuned controls appear in Table~\ref{tab:controls}.}
\label{fig:predictive}
\end{figure}

\subsection{First-error localization remains weak}

The hidden score localizes $28.9\%$ of erroneous traces exactly, $59.3\%$ within one step, and
$72.7\%$ within two. It crosses somewhere on $88.7\%$ of erroneous traces, but also on $38.8\%$
of fully correct traces, and among detections it is late by $+0.59$ steps on average
($[+0.36,+0.80]$). Process F1 is $0.393$ ($[0.351,0.436]$); complete-trace accuracy is $0.404$.

Post-hoc temporal tests show more alignment than these threshold metrics alone suggest. Exact
localization is $0.289$ versus $0.164$ under circular shifts ($p=0.0002$). Across 380 noninitial
errors, the mean onset jump is $0.257$; matched correct transitions rise $0.113$, for a paired
difference of $0.144$ ($[0.096,0.193]$). The score continues from $0.341$ immediately before the
error to $0.559$ at onset, $0.680$ one step later, and $0.731$ two steps later: a gradual
transition into a persistent state, not a sharp alarm. The transition task excludes 52 test traces
whose first error occurs at step 0, which may differ systematically from the 380 eligible ones.

The matched transition probe reaches AUROC $0.769$ ($[0.713,0.820]$), average precision $0.741$
($[0.672,0.816]$), and paired accuracy $0.753$ ($[0.676,0.824]$); position, destination-step
TF--IDF, and shuffled-label controls reach $0.63$, $0.67$, and $0.58$. Given the placebo reuse and
retained covariate imbalance documented above, this supports decodability of an onset-related
change on the already-inspected split; sensitivity refits under one-to-one and inverse-reuse
matching, and replication on untouched data, are needed before any confirmatory reading.

\subsection{Source ranking transfers; model generalization is untested}

Separate probes trained on one ProcessBench source and tested on another have mean off-diagonal
AUROC $0.805$ versus $0.845$ on the diagonal; every off-diagonal cell exceeds $0.739$. Thresholded
Process F1 is less stable: $0.261$ off diagonal versus $0.336$ on diagonal, with individual
off-diagonal values from $0.055$ to $0.371$. These cells show cross-source ranking within one
benchmark construction, not transfer to another model size, family, or dataset.

\subsection{Scope of the evidence}

This paper is a one-model, one-benchmark evaluation audit. The nested comparison shows that hidden
states add held-out predictive information beyond tuned nuisances on the inspected split, but the
evidence is conditional on one frozen split, one model, and one benchmark construction. It does
not measure prevalence across models, establish causal use, or validate self-monitoring. A
second-model replication under the frozen protocols reported here is deliberately left as follow-up
work.

\section{Limitations, responsible use, and conclusion}

This is a one-model, one-benchmark case study; the title and claims name \model{}. The model reads
fixed traces produced by separate generators; it does not generate the evaluated solutions. All
added analyses---transition probe, temporal randomization, shortcut and joint controls, nested
comparisons, trace-equal refits, subgroups---are post-hoc, and all intervals are conditional on
one frozen split and the completed selection pipeline: they capture resampling uncertainty, not
split or selection sensitivity. No dated preregistration exists. Corrected/error twins lack
independent verification, so no patching result is reported; a stronger follow-up would verify
twins, reserve an untouched onset set, and validate the readout first.

The detector is unsuitable for grading, certification, or unattended feedback: its $38.8\%$
false-alarm rate on correct traces could penalize valid work, its late detections could let an
invalid derivation propagate, and its sensitivity to source, outcome, and length makes
distribution shift especially risky. Positive uses are limited to research diagnostics with human
or formal verification.

The central empirical finding is a gap with a precise shape: in \model{} reading \dataset{} traces,
invalid-so-far prefixes are linearly decodable, and adding hidden states to an equally tuned
nuisance model improves held-out ranking and proper scores on the inspected split; what remains is
a better erroneous-trace onset-localization score but worse correct rejection, both weak in
absolute terms. The evidence is model-, split-, and benchmark-conditional. Replication on another
model and an untouched split, and a contextual text baseline that does not rely on bag-of-words
features, are required before attributing the gain to information unavailable from visible
semantics.

For discovery practice, this audit reduces to a checklist. Before treating a decodable internal
pattern as discovered knowledge, test and report: (1) whether adding hidden features to equally
tuned nuisance models---under the same selection budget---improves held-out performance; (2)
whether any residual advantage survives on the task-specific metrics that matter, with paired
intervals; (3) whether temporal correspondence holds against matched and shifted controls; (4)
whether calibration and subgroup behavior survive the shifts the use case implies; and (5) whether
a behavioral readout distinguishes the relevant states before any causal claim. Our candidate
completes the nested test in (1) on the inspected split, shows metric-specific rather than
uniformly strong performance in (2)--(4), and fails the behavioral-readout requirement in (5).

\bibliographystyle{plainnat}
\bibliography{references}

\appendix

\section{Additional data accounting}
\label{app:data}

\begin{table}[h]
\centering
\small
\caption{Data flow. Attempted / retained traces / exclusions by source and split; boundary counts
are written-step boundaries.}
\label{tab:dataflow}
\begin{tabular}{lrrrrrr}
\toprule
Split & Attempted & Retained & Excluded & Boundaries & Error traces & Correct traces \\
\midrule
Train & 2,041 & 2,014 & 27 & 14,944 & 1,311 & 703 \\
Validation & 681 & 677 & 4 & 4,980 & 444 & 233 \\
Test & 678 & 669 & 9 & 4,985 & 432 & 237 \\
\midrule
All & 3,400 & 3,360 & 40 & 24,909 & 2,187 & 1,173 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\small
\caption{Attempted / retained traces / exclusions by source and split.}
\begin{tabular}{lrrrr}
\toprule
Split & GSM8K & MATH & OlympiadBench & Omni-MATH \\
\midrule
Train & 236/236/0 & 597/592/5 & 602/594/8 & 606/592/14 \\
Validation & 80/80/0 & 198/198/0 & 204/202/2 & 199/197/2 \\
Test & 84/84/0 & 205/204/1 & 194/188/6 & 195/193/2 \\
\bottomrule
\end{tabular}
\end{table}

The retained boundary counts by split and source are, respectively: train 1,229 / 3,718 / 5,129 /
4,868; validation 414 / 1,303 / 1,708 / 1,555; and test 439 / 1,387 / 1,661 / 1,498 in the source
order of the table. The original attempted total was 25,697 boundaries; 24,909 remain after the 40
complete-trace exclusions.

\section{Descriptive subgroup audit}
\label{app:subgroups}

\begin{table}[h]
\centering
\small
\caption{Trace-macro outcomes by ProcessBench source (95\% whole-trace bootstrap intervals).}
\begin{tabular}{lrrr}
\toprule
Source & Complete accuracy & Exact error & Correct rejection \\
\midrule
GSM8K & .512 [.405,.619] & .238 [.116,.375] & .786 [.658,.902] \\
MATH & .500 [.426,.569] & .347 [.264,.435] & .723 [.620,.818] \\
OlympiadBench & .298 [.234,.362] & .207 [.138,.282] & .463 [.340,.585] \\
Omni-MATH & .358 [.290,.425] & .324 [.253,.401] & .467 [.319,.614] \\
\bottomrule
\end{tabular}
\end{table}

Among traces with an incorrect final answer, complete accuracy is $0.292$ ($[0.245,0.342]$). Among
traces with a correct final answer it is $0.506$ ($[0.451,0.554]$); this group mixes valid traces and
traces whose reasoning contains an annotated error. Across generators with at least 20 held-out
traces, complete accuracy ranges from $0.316$ to $0.621$. These wide, overlapping estimates are
descriptive evidence of heterogeneity rather than generator rankings.

\section{The failed behavioral pilot}
\label{app:causal}

The original \texttt{CORRECT}/\texttt{INCORRECT} readout has baseline AUROC $0.283$ and specificity
zero. A revised question with conditional Yes/No probabilities remains below chance (AUROC $0.342$,
specificity $0.203$ on 256 balanced boundaries). The intervention code requires both AUROC
$>0.5$ and nonzero specificity before a learned-direction effect is interpreted. Neither readout
passes. Random orthogonal directions cannot substitute for a positive control of assay
sensitivity, so the dose response is not causal evidence for or against behavioral use. We report
the pilot only to delimit the evidence: no directional intervention was interpreted, so the causal
section of the audit is empty by design rather than by null result.

\section{Reproduction record}
\label{app:repro}

The dataset SHA-256 is
\texttt{447f0a4b35c5747a9f9a3dab1e70d43}\allowbreak
\texttt{f71efd501497b8cec668b71337099784a}.
All stochastic analysis uses seed 42. Exact resolved YAML files and artifact identity files are
included with the anonymous supplement. The principal commands are:
\begin{verbatim}
uv sync --extra dev
uv run math-error-tracing \
  --config configs/experiment1.yaml validate-config
uv run math-error-tracing \
  --config configs/experiment1.yaml run-all
uv run math-error-cpu-followup \
  --config configs/experiment2.yaml
uv run math-error-extended-followup \
  --config configs/experiment3.yaml \
  fit-transition-probe
uv run math-error-extended-followup \
  --config configs/experiment3.yaml \
  analyze-transition-matching
uv run pytest
uv run ruff check src tests
\end{verbatim}

\begin{table}[h]
\centering
\small
\caption{Compute and licensing record.}
\label{tab:compute}
\begin{tabular}{lp{0.72\linewidth}}
\toprule
Extraction and interventions & one NVIDIA A100 (40\,GB), bfloat16, batch 16; single passes over \\
 & 3,400 traces; $\sim$hours of GPU time \\
Probe fitting and audits & CPU only; the largest stage bootstraps 2,000 trace resamples \\
Storage & $\sim$1\,GB compact predictions plus shards for raw activations \\
Locked versions & Python 3.10+; PyTorch 2.13.0; Transformers 4.57.6; scikit-learn 1.7.2; \\
 & NumPy 2.2--2.5; pandas 2.3.3; SciPy 1.15--1.18; datasets 4.8.5 \\
Licenses & \dataset{}: Apache 2.0. \model{}: Apache 2.0. Analysis code: repository license. \\
 & Benchmark sources retain their original terms. \\
\bottomrule
\end{tabular}
\end{table}

Activation extraction and the original intervention used one A100 GPU; probe fitting and all audit
analyses are CPU-capable. Extraction is sharded and resumable. The code rejects mismatched dataset,
model, dtype, and context-length identities and stores all selected thresholds and predictions.
The supplement records the exact environment of each analysis stage in its machine-readable
summary files.

\newpage
\input{checklist.tex}

\end{document}
