# ProtoMech asset manifest

Do not commit model weights here. Before running circuit experiments, record:

- repository URL:
- pinned Git commit:
- checkpoint source/version:
- checkpoint SHA-256:
- ESM-2 model identifier and revision:
- CLT layers and latent dimensions:
- download date:
- license/usage notes:

The experiment is not reproducible until these fields are filled in.
